image: Ubuntu  # образ для сборки
build_script:FunctionCall
           - ./gradlew test -Dselenide.headless=true --info functionCall                    
branches: test
  only:
- masternветка git
build: off  # будем использовать св0й скрипт build
install:
  # 3апускаем SUT (& означает, что в фоновом режиме не блoкируем терминал для запуска тестов)
  - java -jar ./artifacts/app-card-delivery.
  - chmod +х gradlew
build_script:FunctionCall
           - ./gradlew test filetest --info functionCall
